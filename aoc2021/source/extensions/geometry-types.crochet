% crochet

type point2d(global x is numeric, global y is numeric);

type line(global start is point2d, global stop is point2d);

type rectangle(
  global left is numeric,
  global top is numeric,
  global width is numeric,
  global height is numeric,
);